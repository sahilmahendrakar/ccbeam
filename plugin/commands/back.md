---
description: Return this conversation to the machine and folder it started from
allowed-tools: mcp__ccteleport__back
---

Call the `mcp__ccteleport__back` tool now, then reply with a single short line — something like "Heading back."

Do not call any other tool and do not summarise the conversation. The session is moved for you the moment your turn ends.
