{
  "description": "Ends the session at the turn boundary when a beam has been requested, so the supervisor can move it. Inert unless /ccbeam:up was used.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/stop.mjs\""
          }
        ]
      }
    ]
  }
}
